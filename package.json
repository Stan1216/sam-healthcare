{
  "name": "sam-healthcare-platform",
  "version": "1.0.0",
  "private": true,
  "description": "SAM Healthcare website + backend (patient OTP login, clinician login, booking capture)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "better-sqlite3": "^11.8.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
